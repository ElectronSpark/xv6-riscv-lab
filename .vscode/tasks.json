{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "configure cmake",
            "type": "shell",
            "command": "/bin/bash",
            "args": [
                "-c",
                "rm -rf ${workspaceFolder}/build-debug; mkdir -p ${workspaceFolder}/build-debug; cd ${workspaceFolder}/build-debug; cmake .. -DOPT_LEVEL=2"
            ],
            "problemMatcher": [],
            "options": {
                "env": {
                    "LAB": "fs",
                    "CPUS": "1"
                },
                "cwd": "${workspaceFolder}/"
            }
        },
        {
            "label": "configure cmake (debug)",
            "type": "shell",
            "command": "/bin/bash",
            "args": [
                "-c",
                "rm -rf ${workspaceFolder}/build-debug; mkdir -p ${workspaceFolder}/build-debug; cd ${workspaceFolder}/build-debug; cmake .. -DOPT_LEVEL=g"
            ],
            "problemMatcher": [],
            "options": {
                "env": {
                    "LAB": "fs",
                    "CPUS": "1"
                },
                "cwd": "${workspaceFolder}/"
            }
        },
        {
            "label": "compile xv6 and run in debug mode",
            "type": "shell",
            "command": "make",
            "isBackground": true,
            "args": [
                "-j16",
                "qemu-gdb"
            ],
            "problemMatcher": {
                "owner": "custom",
                "fileLocation": "relative",
                "pattern": {
                    "regexp": "^.*$",
                },
                // Minimal matcher (never matches) so the object is valid
                "background": {
                    "activeOnStart": true,
                    "beginsPattern": ".*In lab: .*",
                    "endsPattern": ".*Now run gdb in another window.*"
                }
            },
            "options": {
                "cwd": "${workspaceFolder}/build-debug"
            },
            "dependsOn": [
                "configure cmake (debug)"
            ]
        }
    ]
}