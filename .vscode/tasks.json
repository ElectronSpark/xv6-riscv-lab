{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "configure cmake",
            "type": "shell",
            "command": "/bin/bash",
            "args": [
                "-c",
                "rm -rf ${workspaceFolder}/build; mkdir -p ${workspaceFolder}/build; cd ${workspaceFolder}/build; cmake .."
            ],
            "problemMatcher": [],
            "options": {
                "env": {
                    "LAB": "fs",
                },
                "cwd": "${workspaceFolder}/"
            }
        },
        {
            "label": "run cargo geiger",
            "type": "shell",
            "command": "cargo geiger --target=riscv64gc-unknown-none-elf",
            "options": {
                "cwd": "${workspaceFolder}/kernel"
            }
        },
        {
            "label": "compile xv6 and run in debug mode",
            "type": "shell",
            "command": "make",
            "isBackground": true,
            "args": [
                "-j16",
                "qemu-gdb"
            ],
            "problemMatcher": {
                "owner": "custom",
                "fileLocation": "relative",
                "pattern": {
                    "regexp": "^.*$",
                },
                // Minimal matcher (never matches) so the object is valid
                "background": {
                    "activeOnStart": true,
                    "beginsPattern": ".*In lab: .*",
                    "endsPattern": ".*Now run gdb in another window.*"
                }
            },
            "options": {
                "cwd": "${workspaceFolder}/build"
            },
            "dependsOn": [
                "configure cmake"
            ]
        }
    ]
}