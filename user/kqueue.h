/*
 * kqueue.h - User-space kqueue definitions
 *
 * This header re-exports the kernel kqueue ABI (struct kevent, filter
 * constants, flag constants) for user programs.
 */
#ifndef _USER_KQUEUE_H
#define _USER_KQUEUE_H

#include "kernel/inc/kqueue.h"

#endif /* _USER_KQUEUE_H */
